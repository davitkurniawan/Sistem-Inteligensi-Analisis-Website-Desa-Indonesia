services:
  - type: web
    name: cms-desa-detector-backend
    runtime: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
